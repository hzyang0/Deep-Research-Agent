# LLM 调用(OpenAI 兼容接口;换 Claude/本地模型只需改 base_url 和模型名)
openai>=1.0.0
# 网页抓取
requests>=2.31.0
beautifulsoup4>=4.12.0
# 免费搜索(无需 API key)
ddgs>=9.0.0
# 测试
pytest>=8.0.0
# Web 界面后端(FastAPI + SSE)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
# .env 自动加载(可选;没装也能用内置降级解析器)
python-dotenv>=1.0.0